<div class="container">
<h1>Restaurants list</h1>
<% @restaurants.each do |restaurant| %>
  <h2><%= link_to restaurant.name, restaurant_path(restaurant)%></h2>
<% end %>
<%= link_to 'Add new restaurant', new_restaurant_path, class:"btn btn-primary bi bi-plus-lg" %>
</div>

